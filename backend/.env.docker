# Database URL (gunakan nama service 'db' sebagai hostname di Docker network)
DATABASE_URL=postgresql://postgres:postgres123@db:5432/simcuti

# JWT Configuration
SECRET_KEY=simcuti-docker-secret-key-ganti-sebelum-production-2026
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=720

# CORS — izinkan akses dari frontend port 3000 (Nginx) dan 5173 (dev)
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173
